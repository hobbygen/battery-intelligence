<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BatteryIntelligence.App.Controls.MeasurementRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        One "Label: Value [Badge]" line. The badge (Style="GradeBadgeBorderStyle")
        only appears for Calculated/Estimated/Suspect values — specification
        section 3: measured values are shown plainly, so a badge on every line
        would make the one badge that matters invisible.
    -->
    <Grid Padding="0,4" ColumnSpacing="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="90" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Column="0"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="{x:Bind Label, Mode=OneWay}"
            TextWrapping="Wrap" />

        <TextBlock
            Grid.Column="1"
            IsTextSelectionEnabled="True"
            Text="{x:Bind Value.Text, Mode=OneWay}"
            TextWrapping="Wrap" />

        <Border
            Grid.Column="2"
            Style="{StaticResource GradeBadgeBorderStyle}"
            Visibility="{x:Bind Value.RequiresBadge, Mode=OneWay}">
            <TextBlock Style="{StaticResource GradeBadgeTextStyle}" Text="{x:Bind Value.BadgeText, Mode=OneWay}" />
        </Border>
    </Grid>

</UserControl>
