<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BatteryIntelligence.App.Controls.ToggleRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="0,8" ColumnSpacing="14" BorderBrush="{ThemeResource AppLine2Brush}" BorderThickness="0,0,0,1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
            <TextBlock FontSize="13.5" FontWeight="SemiBold" Text="{x:Bind Header, Mode=OneWay}" />
            <TextBlock Style="{StaticResource MetricLabelTextStyle}" TextWrapping="Wrap" Text="{x:Bind Description, Mode=OneWay}" />
        </StackPanel>
        <ToggleSwitch
            x:Name="Switch"
            Grid.Column="1"
            VerticalAlignment="Center"
            MinWidth="0"
            OnContent=""
            OffContent=""
            Toggled="OnToggled" />
    </Grid>

</UserControl>
